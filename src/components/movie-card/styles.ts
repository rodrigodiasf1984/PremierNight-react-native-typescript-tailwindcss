import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    card: { alignItems: "center", marginVertical: 4 },
    title: {
        fontSize: 14,
        fontWeight: "600",
        marginTop: 6,
        textAlign: "center"
    }
})
